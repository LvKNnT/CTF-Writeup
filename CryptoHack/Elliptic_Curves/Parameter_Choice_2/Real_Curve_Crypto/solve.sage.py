

# This file was *autogenerated* from the file solve.sage
from sage.all_cmdline import *   # import sage library

_sage_const_200 = Integer(200); _sage_const_2 = Integer(2); _sage_const_0p5 = RealNumber('0.5'); _sage_const_1 = Integer(1); _sage_const_0 = Integer(0); _sage_const_10 = Integer(10); _sage_const_150 = Integer(150); _sage_const_128 = Integer(128); _sage_const_16 = Integer(16)
import json
from mpmath import mp
from Crypto.Cipher import AES
from Crypto.Util.number import long_to_bytes
from Crypto.Util.Padding import unpad
from sage.all import Matrix, ZZ

# 1. Setup precision
mp.dps = _sage_const_200 

# 2. Load the leaked parameters
output_file = 'output_8d82e413d29d7810ee8eff5d1226453d.txt'
with open(output_file, 'r') as f:
    data = json.load(f)

gx = mp.mpf(data['gx'])
gy = mp.mpf(data['gy'])
px = mp.mpf(data['px'])
py = mp.mpf(data['py'])
ciphertext = bytes.fromhex(data['ciphertext'])
iv = bytes.fromhex(data['iv'])

print("[*] Computing exact elliptic integrals...")

# 3. Exact closed-form solution (No numerical integration)
omega = _sage_const_2  * mp.sqrt(_sage_const_2 ) * mp.ellipk(_sage_const_0p5 )

def get_u(x, y):
    phi = mp.asin(mp.sqrt(_sage_const_2  / (x + _sage_const_1 )))
    val = mp.sqrt(_sage_const_2 ) * mp.ellipf(phi, _sage_const_0p5 )
    if y < _sage_const_0 :
        return omega - val 
    return val

u_G = get_u(gx, gy)
u_P = get_u(px, py)

print(f"[+] Exact u_G: {u_G}")
print(f"[+] Exact u_P: {u_P}")
print(f"[+] Exact omega: {omega}")

# 4. Construct the Lattice
print("[*] Building the lattice...")
SCALE = _sage_const_10 **_sage_const_150 
K_WEIGHT = _sage_const_2 **_sage_const_128  

W_scaled = int(omega * SCALE)
U_G_scaled = int(u_G * SCALE)
U_P_scaled = int(u_P * SCALE)

M = Matrix(ZZ, [
    [W_scaled,   _sage_const_0 , _sage_const_0 ],
    [U_G_scaled, _sage_const_1 , _sage_const_0 ],
    [-U_P_scaled, _sage_const_0 , K_WEIGHT] 
])

L = M.LLL()

# 5. Extract N and Decrypt
N = None
for row in L:
    if abs(row[_sage_const_2 ]) == K_WEIGHT:
        N = abs(row[_sage_const_1 ])
        break

if N is None:
    print("[-] LLL failed to find the scalar.")
    exit()

print(f"[+] Recovered Scalar (N): {N}")

# Reconstruct the 16-byte key
key = long_to_bytes(N).rjust(_sage_const_16 , b'\x00')
print(f"[+] Recovered AES Key: {key.hex()}")

# Decrypt the flag
cipher = AES.new(key, AES.MODE_CBC, iv)
try:
    plaintext = unpad(cipher.decrypt(ciphertext), _sage_const_16 )
    print(f"\n[!] FLAG: {plaintext.decode()}")
except Exception as e:
    print(f"[-] Decryption failed: {e}")

