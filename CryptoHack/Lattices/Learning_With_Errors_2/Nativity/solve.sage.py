

# This file was *autogenerated* from the file solve.sage
from sage.all_cmdline import *   # import sage library

_sage_const_64 = Integer(64); _sage_const_512 = Integer(512); _sage_const_0x10000 = Integer(0x10000); _sage_const_2 = Integer(2); _sage_const_1 = Integer(1); _sage_const_0 = Integer(0)
from sage.all import *
import numpy as np
from random import SystemRandom
from Crypto.Util.number import bytes_to_long, long_to_bytes

n = _sage_const_64 
m = _sage_const_512 
q = _sage_const_0x10000 
dtype = np.uint16

with open("public_key.txt") as f:
    pk = np.loadtxt(f, dtype=dtype)

with open("ciphertexts.txt") as f:
    ciphertexts = np.loadtxt(f, dtype=dtype)

A_full = matrix(Zmod(q), pk[:n, :])
b_full = vector(Zmod(q), pk[n, :])

m_eff = _sage_const_2  * n

A = A_full[:, :m_eff]
A = matrix(ZZ, A_full[:, :m_eff])
b = vector(ZZ, b_full[:m_eff])

Identity_q = q * identity_matrix(ZZ, m_eff)

Lattice = matrix(ZZ, m_eff + n + _sage_const_1 , m_eff + _sage_const_1 )
Lattice.set_block(_sage_const_0 , _sage_const_0 , A)
Lattice.set_block(n, _sage_const_0 , Identity_q)
Lattice.set_block(m_eff + n, _sage_const_0 , matrix(ZZ, _sage_const_1 , m_eff, -b))
Lattice[m_eff + n, m_eff] = _sage_const_1 

print("Running LLL...")
L_reduced = Lattice.LLL()

e_vector = None

for row in L_reduced:
    if row[m_eff] == _sage_const_1 :
        e_vector = vector(ZZ, row[:m_eff])
        break
    elif row[m_eff] == -_sage_const_1 :
        e_vector = vector(ZZ, -row[:m_eff])
        break

if e_vector is None:
    print("Error vector not found!")
    exit(_sage_const_1 )

A_ctx = matrix(Zmod(q), A)                  # Rectangular matrix (64 x m_use)
b_clean = vector(Zmod(q), b + e_vector) # Vector length m_use

try:
    # Solve s * A = b_clean
    s = A_ctx.solve_left(b_clean)
    print("Secret s recovered successfully!")
except ValueError:
    print("Could not solve linear system. Try increasing m_use.")
    exit()

# 7. Decrypt
sk = vector(ZZ, list(-s) + [_sage_const_1 ])

print("Decrypting...")
msg_bits = []
for c in ciphertexts:
    c_vec = vector(ZZ, c)
    # LWE Decryption: <sk, c> gives (message + even_error)
    # Modulo q, then Modulo 2 recovers message
    decrypted_val = sk.dot_product(c_vec) % q
    bit = decrypted_val % _sage_const_2 
    msg_bits.append(bit)

# 8. Reconstruct Flag
flag_int = _sage_const_0 
for bit in msg_bits:
    flag_int = (flag_int << _sage_const_1 ) | int(bit)

print(long_to_bytes(flag_int).decode(errors='ignore'))

