

# This file was *autogenerated* from the file solve.sage
from sage.all_cmdline import *   # import sage library

_sage_const_13415 = Integer(13415); _sage_const_21888242871839275222246405745257275088696311157297823662689037894645226208583 = Integer(21888242871839275222246405745257275088696311157297823662689037894645226208583); _sage_const_1 = Integer(1); _sage_const_2 = Integer(2); _sage_const_0 = Integer(0); _sage_const_5 = Integer(5)#!/usr/bin/env sage

import json
import socket
import sys


HOST = "socket.cryptohack.org"
PORT = _sage_const_13415 

p = _sage_const_21888242871839275222246405745257275088696311157297823662689037894645226208583 
G1 = (_sage_const_1 , _sage_const_2 , _sage_const_1 )


class JsonLineSocket:
    def __init__(self, host, port):
        self.sock = socket.create_connection((str(host), int(port)))
        self.file = self.sock.makefile("rwb", buffering=_sage_const_0 )

    def recvline(self):
        return self.file.readline()

    def recvuntil(self, marker):
        data = b""
        while marker not in data:
            chunk = self.file.read(_sage_const_1 )
            if not chunk:
                raise EOFError("connection closed before marker")
            data += chunk
        return data

    def sendline(self, data):
        self.file.write(data + b"\n")

    def close(self):
        self.file.close()
        self.sock.close()


def recv_json(io):
    line = io.recvline()
    if not line:
        raise EOFError("connection closed")
    return json.loads(line.decode())


def send_json(io, obj):
    io.sendline(json.dumps(obj).encode())


def point_to_server(point):
    return "({}, {}, {})".format(*point)


def solve(host=HOST, port=PORT):
    io = JsonLineSocket(host, port)
    io.recvuntil(b"Welcome! Have fun with this strange implementation...\n")

    # power = p - 5 gives x^(power + 7) - x^3 = x^(p + 2) - x^3 = 0
    # for nonzero x by Fermat. The buggy inverse(0, p) returns 0, so x*z = 0.
    send_json(io, {"option": "set_internal_z", "z": hex(p - _sage_const_5 )})
    response = recv_json(io)
    if "error" in response:
        raise RuntimeError(response["error"])

    send_json(io, {
        "option": "do_proof",
        "G": point_to_server(G1),
        "hsh": "0x1",
    })

    result = recv_json(io)
    io.close()
    print(result.get("msg", result))


if __name__ == "__main__":
    host = sys.argv[_sage_const_1 ] if len(sys.argv) > _sage_const_1  else HOST
    port = int(sys.argv[_sage_const_2 ]) if len(sys.argv) > _sage_const_2  else PORT
    solve(host, port)

